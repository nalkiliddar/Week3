pipeline {

    agent { label 'docker-agent' }

    stages {

        stage("BUILD") {
          
            steps {
                    echo 'this is a test branch'
            }
        }
        
    }
}

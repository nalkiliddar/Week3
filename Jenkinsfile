pipeline {

    agent { label 'docker-agent' }

    stages {

        stage("Build") {
            
            steps {
                    echo 'this is the main branch'
            }
        }
        
    }
}
